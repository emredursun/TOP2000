@model IEnumerable<Top2000.Models.Lijst>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Positie)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Song.Titel)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Top2000Jaar1.Titel)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Positie)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Song.Titel)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Top2000Jaar1.Titel)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
